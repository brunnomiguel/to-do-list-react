import styled, { css } from "styled-components";

export const Container = styled.main`
  ${css`
    width: 100%;

    ul {
      max-width: 600px;
      width: 100%;
      margin: 0 auto;
      padding: 20px;
    }
  `}
`;
